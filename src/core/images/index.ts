/**
 * Images module - placeholder for future image utilities.
 *
 * Image handling is now simplified:
 * - Base64 data is stored directly in ImageAttachment.data
 * - No caching or hydration needed
 * - Rendering converts base64 to data URI directly
 */

// This module is intentionally empty - image data is now stored inline
